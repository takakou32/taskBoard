# --- 各環境ごとの設定（NASパスなどが入るため共有しない） ---
config.local.json

# --- WebView2 の DLL（NuGet から取得するもの。lib/README.md の手順を参照） ---
lib/*.dll
lib/runtimes/

# --- 実行時に自動生成されるもの ---
# 書き込み調停用のロック
**/.locks/
# 日次の世代バックアップ
**/backup/
# 一時ファイル（書き込み途中の中間ファイル）
*.json.tmp

# --- 実データ ---
# 本番のデータはNAS上に置く。リポジトリにはサンプルだけを含める。
data/live/
